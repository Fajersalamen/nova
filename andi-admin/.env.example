# Same Supabase project as the andi/ mobile app.
SUPABASE_URL=https://your-andi-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Desk-only access gate for this MVP admin (see ARCHITECTURE note in andi/ARCHITECTURE.md
# section 6 — real admin auth via admin_users + Supabase Auth is a follow-up, not required
# for a tool only ops staff have the URL to).
ADMIN_PASSWORD=change-me
