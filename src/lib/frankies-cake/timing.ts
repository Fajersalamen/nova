/** Shared timing so the hero's entrance is calibrated to finish exactly as the loader lifts. */
export const LOADER_HOLD_MS = 900;
export const LOADER_EXIT_MS = 600;
export const LOADER_TOTAL_S = (LOADER_HOLD_MS + LOADER_EXIT_MS) / 1000;
